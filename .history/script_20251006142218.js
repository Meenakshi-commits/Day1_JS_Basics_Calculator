const display = document.getElementById('display');
const numuttons = document.querySelectorAll(".num");
const opbuttons = document.querySelectorAll(".op");
const eqlbutton = document.getElementById("equals");
const cl

const